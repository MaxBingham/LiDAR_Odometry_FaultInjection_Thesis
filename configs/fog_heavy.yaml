# Heavy Fog Configuration
# Significant visibility degradation
# Parameters epsilon, a, b, lambda_param are computed from visibility using chamfer metric
fog:
  visibility: 20.0   # meters - heavy fog
